import Spinner from 'react-bootstrap/Spinner';

function Loading() {
  return <Spinner animation="grow" />;
}

export default Loading;

/*
const Loading = () => {
  return (
    <p>Cargando...</p>
  )
}

export default Loading */